<% if @user %>
<%= image_tag @user.avatar.url %> 

<%= form_for(@user) do |f| %>

<%= f.label :avatar %>
<%= f.file_field :avatar %>

<%= f.submit "Update Avatar", class: "btn btn-large btn-primary"%>

<% end %>

<% end %> 