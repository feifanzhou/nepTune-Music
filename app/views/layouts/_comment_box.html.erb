
<div class='Comment-form'>
  <%= form_for Comment.new do |f| %>
  <%= f.hidden_field :comment_id, value: reply_id if reply_id%>
  <%= f.text_area :text %>
  <% end %>
  
  <div class='buttons'>
    <button class='btn' id="submit">Comment</button>
    <% if reply_id %>
    <button class='btn' id="cancel">Cancel</button>
    <% end %>
  </div>
</div>


