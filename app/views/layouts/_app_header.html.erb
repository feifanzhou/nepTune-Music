<div id='pageHeader'>
  <div id='pageHeaderContainer'>
    <ul id='headerNavLinks'>
      <li><strong><%= link_to 'Karmonix', root_path %></strong></li>
      <% if !cookies[:current_user].blank? %>
        <% cu = User.find_by_remember_token(cookies[:current_user]) %>
        <% bms = BandMember.where("user_id = #{ cu.id }") %>
        <% bms.each do |bm| %>
          <% an = Artist.find(bm.artist_id).artistname %>
          <li><%= link_to an, artist_main_path(an) %></li>
        <% end %>
      <% end %>
      <li>Me</li>
    </ul>
    <%= render 'layouts/playbar' %>

    <% if cookies[:current_user].blank? %>
    <div id='headerLogin'><%= link_to 'Login', login_path(), class: 'btn' %></div>
    <% else %>
    <div class="btn-group pull-right" id='headerActions'>
      <a class="btn dropdown-toggle Icon" data-toggle="dropdown" href="">
        &#59415;
        <span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
        <li><%= link_to 'Logout', logout_path() %></li>
      </ul>
    </div>
    <% end %>
      
    <form id='headerSearch'>
      <input type='text' id='headerSearchField' placeholder='Search for people, songs, and events'/>
    </form>
  </div>
</div>